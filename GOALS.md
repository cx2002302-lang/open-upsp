# open-upsp 功能目标分析

> **⚠️ 内部文档 | DO NOT publish to GitHub**
> 
> 分析日期: 2026-05-12  
> 用途: 明确项目从功能上能达成什么，不能达成什么，为质问环节提供依据。

---

## 一、open-upsp 是什么（功能定义）

open-upsp 是一个**位格（Persona）管理工具**，它让 AI 代理拥有：

1. **可定义的身份** — 通过七文件描述"我是谁"（认知风格、行为规则、术语体系）
2. **可读取的记忆** — 通过 SQLite 桥接读取 Zettelkasten 知识库
3. **可组装的上下文** — 将身份 + 状态 + 记忆 + 知识组装成 AI 可用的 prompt

**它不是**：
- 一个 AI 模型
- 一个自动运行的 Agent
- 一个替代 Zettelkasten 的系统
- 一个云端服务

---

## 二、功能边界 — 能做到什么

### 2.1 Phase 1（只读桥接）— 确定能做到

| 功能 | 描述 | 输入 | 输出 |
|------|------|------|------|
| 初始化位格 | 从模板创建七文件 | `open-upsp init` | `~/.openclaw/openupsp/personas/default/` 下的七文件 |
| 查看位格状态 | 读取 core.md + state.json | `open-upsp status` | 格式化的状态报告 |
| 搜索知识库 | 查询 ZK 数据库的笔记 | `open-upsp search "关键词"` | 匹配的笔记列表 |
| 构建上下文 | 组装七文件 + 搜索结果 | `open-upsp context` | 一段结构化的文本（可直接注入 AI prompt） |
| 手动更新状态 | 修改 state.json | `open-upsp state update` | 更新后的 state.json |

**Phase 1 的价值**：用户可以在每次与 AI 对话前，运行 `open-upsp context`，将位格定义和知识库检索结果复制粘贴到 AI 的 system prompt 中。AI 因此"知道"自己是谁，以及知道用户之前积累的知识。

### 2.2 Phase 2（双向写入）— 计划做到

| 功能 | 描述 | 前提 |
|------|------|------|
| 会话蒸馏 | 将会话日志解析为 STM 条目 | 需要会话日志文件 |
| 自动同步到 ZK | 高权重 STM → ZK FLEETING 笔记 | 需要写入权限 |
| OpenClaw Skill | 在 Agent 对话中自动调用 open-upsp | 需要 OpenClaw Skill 机制 |

### 2.3 Phase 3（关系融合）— 远期目标

| 功能 | 描述 | 前提 |
|------|------|------|
| 共振度映射 | UPSP relation.md ↔ ZK 链接 confidence | 需要 ZK 扩展链接元数据 |
| 自动状态更新 | 对话中动态调整动态六轴 | 需要与 OpenClaw 深度集成 |
| 多态协作 | 多个位格共享同一 ZK | 需要架构扩展 |

---

## 三、功能边界 — 做不到什么

| 功能 | 为什么做不到 | 替代方案 |
|------|------------|---------|
| 自动在后台运行 | open-upsp 是 CLI 工具，不是守护进程 | 用户手动运行，或配合 cron/Skill |
| 直接与 AI 模型通信 | 不是 LLM 客户端，不调用 API | 输出上下文文本，用户自行粘贴到 AI 对话中 |
| 替代 Zettelkasten | 只读桥接，不修改 ZK 核心 | 与 ZK 协作，而非替代 |
| 跨设备同步 | 纯本地存储，无云端 | 用户自行用 git/rsync 同步 `~/.openclaw/openupsp/` |
| 自动判断"什么值得记住" | 没有内置 LLM，不做内容理解 | Phase 2 依赖外部 LLM 进行蒸馏 |
| 实时情绪识别 | 无法读取对话中的情绪 | 需要人工或通过其他系统更新 state.json |
| 防止 AI 身份被覆盖 | 只能在 prompt 中声明规则，无法强制模型遵守 | 依赖"身份声明在 system prompt 中"这一机制 |

---

## 四、用户价值链条

```
用户编辑 core.md → 定义位格身份
         ↓
用户与 AI 对话前运行 open-upsp context
         ↓
open-upsp 读取七文件 + 搜索 ZK 知识库
         ↓
输出一段结构化文本（身份 + 状态 + 知识）
         ↓
用户将文本粘贴到 AI 的 system prompt
         ↓
AI 以"有记忆、有身份"的方式回应
         ↓
对话结束后（Phase 2）→ 有价值的内容自动流入 ZK
```

**核心价值**：
- 解决"每次对话 AI 都失忆"的问题
- 解决"AI 没有稳定性格"的问题
- 让个人知识库（ZK）真正成为 AI 的"长期记忆"

---

## 五、质问环节依据

基于以上分析，以下是可能存在的"预期落差"，需要通过质问来验证：

1. **使用方式**：用户是否接受"手动运行 CLI → 复制粘贴到 AI"这一工作流？还是期望全自动集成？
2. **AI 集成深度**：用户使用的 AI 系统是否支持自定义 system prompt？（OpenClaw 支持，但其他系统如 ChatGPT Web 版不支持）
3. **知识库规模**：用户的 ZK 中有多少笔记？如果为空，Phase 1 的价值会大打折扣。
4. **多设备**：用户是否需要在多台机器上使用？纯本地存储需要自行同步。
5. **自动化期望**：用户是否期望 open-upsp 自动判断"什么值得记住"并自动写入 ZK？这需要 LLM，Phase 1 做不到。
6. **位格数量**：用户需要多少个位格？默认 1 个是否足够？
7. **AI 平台**：用户主要在哪个平台与 AI 对话？（OpenClaw CLI / Web / 其他）
