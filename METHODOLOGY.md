# open-upsp 项目方法论

> **⚠️ 内部文档 | DO NOT publish to GitHub**
> 
> 用途：记录项目启动前的标准流程，防止预期落差，确保开发方向与真实需求对齐。

---

## 一、为什么需要这个方法论

AI 项目的失败往往不是技术问题，而是**预期落差**问题：

- 开发者以为用户要 A，用户其实想要 B
- 用户以为项目能做到 X，实际只能做到 Y
- 双方对"MVP"的定义完全不同

本方法论通过**目标分析 → 深度质问 → 预期落差识别 → 边界确认**四步流程，在项目写第一行代码之前，就把方向对齐。

---

## 二、标准流程（四步）

### Step 1: 功能目标分析

**目标**：明确项目"能做到什么"和"做不到什么"。

**输出物**：`GOALS.md`

**内容模板**：
1. **功能定义** — 一句话说明这是什么
2. **功能边界（能做到）** — 按 Phase 列出确定能做到的功能
3. **功能边界（做不到）** — 明确列出做不到的功能及理由
4. **用户价值链条** — 从用户动作到最终价值的完整链路
5. **质问依据** — 基于以上分析，列出可能存在的预期落差点

**执行标准**：必须诚实。"做不到"的部分要明确写出，不能含糊。

---

### Step 2: 深度质问

**目标**：通过结构化质问，挖掘用户的真实需求和深层期望。

**原则**：
- 不问"能不能"（用户通常说"能"），问"你期望的最理想场景是什么"
- 提供具体选项，但不限制用户（保留"其他"空间）
- 追问功能性细节（"特殊效果""wow 时刻"），而非技术实现

**质问维度**（根据项目调整）：

| 维度 | 示例问题 |
|------|---------|
| **使用方式** | 手动运行 / 后台自动 / Skill 集成？ |
| **平台环境** | 在哪个系统/平台中使用？ |
| **现状基础** | 已有数据/基础设施是什么状态？ |
| **自动化程度** | 全自动 / 半自动 / 手动？ |
| **角色定位** | 希望 AI 扮演什么角色？ |
| **特殊效果** | 想象一个最理想的"wow 时刻" |
| **内容类型** | 数据/知识的类型和规模？ |
| **长期演化** | 一成不变 / 缓慢演化 / 自主成长？ |

**输出物**：质问记录（问答文本）

---

### Step 3: 预期落差识别

**目标**：将 Step 1 和 Step 2 的结果对比，找出"用户期望"与"项目现实"之间的差距。

**分析框架**：

```
用户期望          项目现实           差距           应对方案
─────────        ─────────        ───────        ─────────
全自动 Skill      Phase 1 只有 CLI   Phase 差距     明确时间线
自主成长          受控微调           边界差距       定义阈值约束
情绪感知          只能存储情绪状态    能力差距       明确外部依赖
```

**输出物**：落差分析表（可写入 BRIEFING.md 或 GOALS.md 的附录）

**执行标准**：
- 每个差距必须有明确的"应对方案"
- 如果差距无法弥合，必须在开发前告知用户
- 不能为了"满足预期"而承诺技术上做不到的事情

---

### Step 4: 边界确认

**目标**：与用户共同划定"自主"的边界，防止无限期望膨胀。

**关键对话**：
- "你期望的'自主'到什么程度？"
- "什么必须由你控制，什么可以交给系统？"
- "如果系统做了你不喜欢的改变，如何回滚？"

**输出物**：边界声明（写入项目文档）

**示例**（本项目）：

> **自主成长边界声明**（v1.0）
> 
> open-upsp 支持"有限的自主成长"，具体边界如下：
> 
> | 层级 | 可自主调整 | 调整限制 | 需用户确认 |
> |------|-----------|---------|-----------|
> | 动态六轴 | ✅ 是 | 每轮 ±5，范围 0~100 | ❌ 否 |
> | 关系矩阵 | ✅ 是 | 每次 ±0.05 | ❌ 否 |
> | 工化指数 | ✅ 是 | 基于公式自动计算 | ❌ 否 |
> | 核心六轴 | ⚠️ 条件 | 需累计 256 轮 + 用户确认 | ✅ 是 |
> | rules.md | ❌ 否 | 仅用户手动修改 | ✅ 是（必须） |
> | core.md 身份 | ❌ 否 | 仅用户手动修改 | ✅ 是（必须） |

---

## 三、本次项目的执行记录

### Step 1: 功能目标分析

- **执行时间**：2026-05-12
- **输出物**：`GOALS.md`
- **关键发现**：
  - open-upsp 是 CLI 工具 + 库，不是 AI 模型或后台服务
  - Phase 1 只能做到手动运行，Skill 自动触发是 Phase 2
  - 做不到"自动判断记忆价值"（需要 LLM，Phase 2）
  - 做不到"情绪识别"（只能存储/传递情绪状态）

### Step 2: 深度质问

- **执行时间**：2026-05-12
- **质问轮次**：2 轮
- **关键回答**：
  - 使用方式：OpenClaw Skill（Phase 2 需求）
  - 位格定位：专业助手 + 情感陪伴（混合模式）
  - 特殊效果："今天比之前更懂我" + "知道我心情不好"
  - 知识库：混合类型，无分类
  - 长期演化：自主成长（最激进选项）

### Step 3: 预期落差识别

- **执行时间**：2026-05-12
- **输出物**：`BRIEFING.md` 第三节
- **发现的落差**：
  1. 自主成长 vs 安全约束（核心六轴不可自主修改）
  2. 情绪感知 vs 情绪存储（open-upsp 不识别情绪）
  3. 混合位格 vs 单模式设计（需要多模式切换机制）
  4. 全自动 vs 手动 CLI（Phase 1 差距）

### Step 4: 边界确认

- **执行时间**：2026-05-12
- **用户决策**：
  - 确认"有限的自主成长"，接受阈值限制
  - 核心身份（core.md / rules.md）必须由用户控制
  - 动态六轴可在约束范围内自主调整
- **输出物**：本文件中的"自主成长边界声明"

---

## 四、何时触发这个方法论

**必须触发**（以下任一条件满足）：
- [ ] 新项目启动
- [ ] 项目目标发生重大变化
- [ ] 用户对功能产生新的期望
- [ ] Phase 转换（如从 Phase 1 进入 Phase 2）

**可选触发**：
- [ ] 每个开发周期开始前复盘
- [ ] 用户反馈与预期不符时

---

## 五、复用指南

对于新项目，按以下顺序执行：

```
1. 阅读项目现有文档（README / AGENTS.md / 分析报告）
2. 编写 GOALS.md（Step 1: 功能目标分析）
3. 设计质问问题（参考"质问维度"表格）
4. 执行质问（记录问答）
5. 对比分析，识别预期落差
6. 与用户确认边界
7. 更新项目文档（AGENTS.md / BRIEFING.md）
8. 开始开发
```

---

## 六、核心原则

1. **诚实优于讨好** — 明确说"做不到"比含糊承诺更负责任
2. **边界即安全** — "自主"必须有阈值，"无限"等于"失控"
3. **文档即契约** — 所有确认的内容必须写入文档，防止后续争议
4. **用户参与设计** — 不是"我告诉你能做什么"，而是"我们一起定义边界"

---

*方法论版本: 1.0.0 | 创建日期: 2026-05-12 | 适用项目: open-upsp*
